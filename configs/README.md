# Configs Directory

Purpose: centralized experiment/training configuration storage.

Rules:
- Keep hyperparameters in a single YAML file for all controlled experiments.
- Do not hardcode hyperparameters in multiple scripts.
- Track config version changes together with experiment records.
